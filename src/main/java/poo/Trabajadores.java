package poo;

public interface Trabajadores {
    double estableceBonus(double gratificacion);//Todos los métodos de las interfaces son public y abstract, no se pone.
    double bonusBase = 1500; //Es una constante, aunque no se indique, todas las variables son public, static y final (esta en cursiva)
}
